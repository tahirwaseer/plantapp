<td><%= user.id%></td>
<td><%= link_to user.name, edit_admin_user_path(user) %></td>
<td><%= user.email %></td>
<td><%= user.effective_start_date %></td>
<td><%= user.effective_end_date %></td>
<td>
	<%= link_to 'Edit', edit_admin_user_path(user) %> |
	<%= link_to 'Delete', admin_user_path(user), method: :delete, data: {confirm: 'Are you sure you want to delete this user?'}%>
	
</td>
